# 	You are given the sorted array which could contain the 
# pos, neg or 0, determine the min abs of this array

# Time complecxity O(n), can't compare in the orderd array.

class Solution(object):
	"""
	return the min abs value of the sorted array
	"""
	def min_abs(self,array):
		if not array:
			return 0
		if array[-1]<=0
		return abs(array[-1])
		